syntax = "proto3";

package api.metadata;

enum BallType {
    DEFAULT = 0;
    BASKETBALL = 1;
    PUCK = 2;
    CUBEBALL = 3;
    BREAKOUT = 4;
}

message Mutators {
    optional BallType ball_type = 1;
    optional int32 game_mutator_index = 2;
}
